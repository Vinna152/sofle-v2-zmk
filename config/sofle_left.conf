# This repo is configured for dongle-mode operation (xiao_ble + sofle_dongle is central).
# Left runs as a peripheral — the keyboard will only work when the dongle is paired and powered.
# To use left as a standalone central instead, delete this file and remove the dongle entries from build.yaml.
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
